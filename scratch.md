

# Feedback

## General

- In commits now and going forward run the notebook (`python3 note.py`) to check it doesn't have errrors or `marimo check`
- Make this notebook more interactive so the user can play with it (ie $d$ should be a slider)

Attention computes: $\widehat{\mathbf{x}}_{m_0 q}= \frac{1}{|\mathcal{I}(m_0)|}\sum_{i \in \mathcal{I}(m_0)}\mathbf{x}_{m_0 i}$