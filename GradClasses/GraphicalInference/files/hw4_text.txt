Homework 4
Due Friday February 27, 2026
Reading:
• Required: Lecture 6 Notes
• Optional: PGMCT Ch. 6 and GMEFVI Ch. 6
Problems:
1. (Learning with a single variable and factor). Let X ∈ [m] := {1, 2, . . . , m} and
consider the one-factor model
m
X

1
pθ (x) =
exp θx ,
Z(θ) =
exp(θu ),
Z(θ)
u=1

where θ = (θ1 , . . . , θm ) ∈ Rm . Given i.i.d. data D = {x(1) , . . . , x(N ) }, define counts
Nx :=

N
X

1{x(ℓ) = x},

pbD (x) :=

ℓ=1

Nx
.
N

(a) Write the normalized log-likelihood N1 L(θ) explicitly in terms of {Nx } and θ.
(b) Compute ∂θ∂x N1 L(θ) and simplify it to the difference between data and model terms.
(c) Show that N1 L(θ) has a maximizer that is not unique due to an extra degree of freedom.
P
(d) Impose the constraint m
x=1 θx = 0. Under this constraint, solve for the MLE θ̂ in closed
form in terms of pbD .
2. (Identifiability and gauge). Consider a factor graph with indicator/table factors
θa ∈ R|X∂a | ,

fa (x∂a ; θa ) = exp(θa,x∂a ),

θa,x∂a = [θa ]x∂a .

Assume the graph is connected and all variables are discrete.
(a) Show that if you add a constant ca to each entry of θa (possibly different across factors),
the distribution pθ (x) does not change. Thus, the constants ca cannot be identified from
data.
(b) Show that there is at least one additional redundancy: a “reparameterization” that
preserves pθ in a pairwise MRF. Specifically, consider a pairwise MRF
X

X
pθ (x) ∝ exp
θi (xi ) +
θij (xi , xj ) .
i∈V

(i,j)∈E

′
Show that we can use arbitrary functions ϕi : Xi → R to define new parameters θi′ , θij
(depending on ϕi ) so that
X
X
X
X
′
θi′ (xi ) +
θij
(xi , xj ) =
θi (xi ) +
θij (xi , xj ) for all x.
i

i

(i,j)

1

(i,j)

(c) Propose a set of constraints that removes these redundancies (not necessarily uniquely),
and briefly argue why it fixes identifiability.
3. (EM soft clustering). Let Y ∈ {1, . . . , m} be observed and H ∈ {1, . . . , K} hidden.
Consider

pθ (y, h) ∝ exp θyh + αy + βh .
One can interpret H as a latent class (or cluster) index and q (ℓ) (h) as a soft cluster assignment
for datum y (ℓ) .
(a) Show that pθ (h | y) is a softmax in the scores θy∗ + β.
(b) For data {y (ℓ) }N
ℓ=1 , write the E-step responsibilities and explain their clustering meaning.
(c) Derive the M-step objective and first-order condition in terms of soft counts.
(d) Describe explicit constraints for (θ, α, β) that remove symmetries to make the parameters
more identifiable.
4. (Small-step viewpoint: EM direction as likelihood gradient.) Let Lobs (θ) =
PN
θ (ℓ)
ℓ=1 ln p (y ) be the observed-data log-likelihood in a latent-variable model with latent H
and complete-data log-density ln pθ (y, h).
Fix a current parameter θ and define the Q-function
Q(θ′ | θ) :=

N
X



′
Epθ (H|y(ℓ) ) ln pθ (y (ℓ) , H) .

ℓ=1

(a) Show that the gradient of the observed-data log-likelihood at θ equals the gradient of
Q(θ′ | θ) with respect to θ′ evaluated at θ′ = θ:
∇θ Lobs (θ) = ∇θ′ Q(θ′ | θ) θ′ =θ .
(Assume you can interchange derivative and summation/expectation.)
(b) Conclude that if you do a small M-step update of the form
θnew = θ + η ∇θ′ Q(θ′ | θ) θ′ =θ

with η > 0 small,

then θnew is a first-order ascent step for Lobs (θ).
(c) (Interpretation) Explain in one paragraph why this supports the intuition that “the
E-step is exact in the small-change limit.”
5. (A Linear-Programming Solver for Sudoku). Combinatorial problems that optimize
over permutations (e.g., scheduling and assignment problems) can be quite challenging computationally. In this problem, we explore low-complexity linear-programming (LP) relaxations.

2

(a) Assume n tasks must be assigned to n agents and let xi,j denote the indicator variable
for “task i assigned to agent j”. If the cost of assigning task i to agent j is ci,j , then the
best assignment problem is equivalent to the integer linear program
X
X
X
min
ci,j xi,j
subject to xi,j ∈ {0, 1},
xi,j = 1,
xi,j = 1.
{xi,j }

i,j

i

j

Solve this problem by hand for n = 4 and


10 7 24 6
 19 8 7 8 


C=
.
 16 22 12 15 
4 8 10 14
Now, solve the linear program where the constraint xi,j ∈ {0, 1} is relaxed to xi,j ∈ [0, 1].
This minimizes average cost when xi,j is the probability of assigning task i to agent j.
(b) Using a similar approach, an N × N Sudoku game (where N is a perfect square) can be
cast an integer linear program with N 3 variables. Let xi,j,k denote the indicator variable
for “the square in row i and column j is assigned the value k”. Based on the previous
part, write the permutation constraints as linear equalities for N = 4.
(c) The Sudoku puzzle is solved by computing
X
min
ci,j,k xi,j,k ,
{xi,j,k }

i,j,k

where ci,j,k be the cost of assigning the integer k to the (i, j)-th entry of the puzzle.
Known values in the puzzle can be handled either adding equality constraints or by
adjusting the cost function. For example, “the square in row i and column j is observed
to be k” implies the equality constraint xi,j,k = 1. Known values can also be included
by initializing ci,j,k to all zeros and then setting ci,j,k = −1 for each (i, j, k) observation.
Choose one of these approaches and use linear programming to solve the N = 4 Sudoku:
2
1
2

2
3
3

(d) Let X = {1, 2, . . . , N } and f : X N → {0, 1} be a factor defined by f (z1N ) = 1 if z1N
defines a permutation of X and 0 otherwise. From this, one can construct the Sudoku
factor graph by introducing N 2 variables and 3N 2 permutation factor nodes. Local 0/1
factors can be used to account for known values. In this case, marginalization gives
X
gi (xi ) =
f (x) = # valid patterns matching all observations with fixed xi .
x\xi

3

Unfortunately, naive implementation of the sum-product algorithm for the permutation
factor requires the summation of N ! terms. Describe how one might implement a factorgraph permutation constraint with much lower complexity. [ Hint: It is fine to introduce
additional variable and factor nodes. ]
Practice Problems (do not hand in):
1. (Bit MAP versus Block MAP Decoding). Consider the binary linear code C defined
by the parity-check matrix


1 1 0 0 0


H =  0 1 1 1 0 .
0 0 0 1 1
(a) List the four codewords in C.
(b) A codeword X = (X1 , . . . , X5 ) ∈ C is chosen uniformly at random from C and transmitted over a binary symmetric channel with cross-over probability p = 1/3. Using the
sum-product algorithm, determine Pr(X3 = 0|Y = (1, 1, 1, 1, 1)), i.e., the probability
that X3 = 0 given that the particular vector Y = (1, 1, 1, 1, 1) is received. Clearly show
the values of messages passed on the edges of the factor graph during each step of the
sum-product algorithm.
(c) Let C0 = {x ∈ C | x3 = 0} and C1 = {x ∈ C | x3 = 1}. Show that
P
Pr(X3 = 0|Y = y)
x∈C Pr(Y = y|X = x)
=P 0
Pr(X3 = 1|Y = y)
x∈C1 Pr(Y = y|X = x)
and use this expression to verify your answer to (b).
(d) Suppose that channel crossover probability in the previous question is changed to p =
2/5. Show that the symbol-wise MAP decision for X3 does not agree with the block-wise
MAP decision for X.
2. (Message passing as reparameterization). Consider a strictly positive pairwise MRF
Y
Y
p(x) ∝
ψi (xi )
ψij (xi , xj ).
i∈V

(i,j)∈E

For each directed edge i → j, let ρi→j : Xj → R>0 be any positive function (note: the
argument is a value zj of the destination variable). Define reparameterized factors
ψi′ (xi ) := ψi (xi )

Y

ρk→i (xi ),

′
ψij
(xi , xj ) := ψij (xi , xj )

k∈∂i

1
.
ρi→j (xj )ρj→i (xi )

(a) Show that this transformation preserves the joint distribution exactly.
(b) Explain why this is a gauge/reparameterization symmetry and how BP fits into it.
4

(c) At a BP fixed point, define beliefs
bi (xi ) ∝ ψi (xi )

Y

mk→i (xi ),

k∈∂i

bij (xi , xj ) ∝ ψij (xi , xj )ψi (xi )ψj (xj )

Y

mk→i (xi )

k∈∂i\{j}

Y

mℓ→j (xj ).

ℓ∈∂j\{i}

Show briefly how fixed-point equations imply local calibration
X
X
bij (xi , xj ) = bj (xj ),
bij (xi , xj ) = bi (xi ).
xi

xj

(d) Explain what is special about the BP fixed-point parameterization.
3. (Consistency of Maximum pseudo-likelihood (MPL) estimation). Let Xi ∈ [q] and
consider a pairwise FG:
X

X
pθ (x) ∝ exp
θi (xi ) +
θij (xi , xj ) .
i∈V

(i,j)∈E

Define the pseudo-log-likelihood to be
N

ℓPL (θ) =

1 XX
(ℓ)
(ℓ) 
ln pθ xi | x∂i .
N
ℓ=1 i∈V

Assume data are i.i.d. from a true parameter θ⋆ (up to gauge) and all conditionals are strictly
positive.
(a) Derive pθ (xi | x∂i ) explicitly in terms of θi and θij .
(b) Compute ∂θi∂(a) ℓPL (θ).
(c) Compute ∂θij∂(a,b) ℓPL (θ) for an edge (i, j) ∈ E.
(d) Let pD be the empirical distribution of the data and define the population pseudolikelihood
X


:=
Lpop
(θ)
EpD ln pθ (Xi | X∂i ) .
PL
i∈V

Now, assume that pD ≈ pθ0 and the above to derive
h 
i
X
pop
θ0
θ
Lpop
(θ
)
−
L
(θ)
=
E
D
p
(X
|
X
)
p
(X
|
X
)
≥ 0.
0
i
i
∂i
∂i
X∂i ∼pθ0
PL
PL
i∈V

Explain what this implies about MPL estimation.
4. (Complexity of learning with BP inner loop.) Consider a tree-structured factor
graph with maximum variable alphabet size q and maximum factor degree d.

5

(a) Give the time complexity (big-O) of one full BP pass (all directed messages) in terms of
n, q, d.
(b) Now consider a single cycle of length n with pairwise factors and alphabet size q. If
loopy BP runs for T iterations, give the complexity.
(c) Explain briefly why learning by gradient ascent on loopy graphs can be far more expensive than on trees even if each BP iteration is cheap.
5. (Learning with a single cycle). Consider an inhomogeneous binary Ising model (xt ∈
{±1}) on an n-cycle:
J,h

p

(x) ∝ exp

n
X

Jt xt xt+1 +

t=1

n
X


ht xt ,

xn+1 ≡ x1 .

t=1

Here each edge has its own coupling Jt and each node its own field ht . Assume N i.i.d.
samples {x(ℓ) }N
ℓ=1 .
(a) Write the normalized log-likelihood N1 L(J, h) and its gradient in componentwise “data
moment minus model moment” form.
(b) Show how to compute all model moments
EpJ,h [Xt ],

EpJ,h [Xt Xt+1 ],

t = 1, . . . , n,

exactly in O(n) time using “break the cycle by conditioning on one spin.”
(c) Give a concrete gradient-ascent algorithm using this exact inference routine.
(d) (Insight) Why is one-cycle learning much easier than 2D-grid learning, despite both
being loopy?

6

